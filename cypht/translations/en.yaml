---
configuration:
  CYPHT_SESSION_TYPE:
    name: "How logged in state is maintained"
    description: "(default: PHP - Not sure why yet but PHP sessions are not working in this image right now, but DB sessions do so I added it to the example docker compose above)"
  CYPHT_AUTH_TYPE:
    name: "Type of user authentication"
    description: "(default: DB)"
  CYPHT_AUTH_USERNAME:
    name: "User account to be created in database for authentication"
    description: "(default: blank)"
  CYPHT_AUTH_PASSWORD:
    name: "Password for the user"
    description: "(default: blank)"
  CYPHT_LDAP_AUTH_SERVER:
    name: "LDAP server (only used if auth_type is LDAP)"
    description: "(default: localhost)"
  CYPHT_LDAP_AUTH_PORT:
    name: "LDAP server port (only used if auth_type is LDAP)"
    description: "(default: 389)"
  CYPHT_LDAP_AUTH_TLS:
    name: "LDAP server encryption (only used if auth_type is LDAP)"
    description: "(default: blank)"
  CYPHT_LDAP_AUTH_BASE_DN:
    name: "LDAP server base dn (only used if auth_type is LDAP)"
    description: "(default: 'example,dc:com')"
  CYPHT_IMAP_AUTH_NAME:
    name: "IMAP server name/label in UI (only used if auth_type is IMAP)"
    description: "(default: localhost)"
  CYPHT_IMAP_AUTH_SERVER:
    name: "IMAP server (only used if auth_type is IMAP)"
    description: "(default: localhost)"
  CYPHT_IMAP_AUTH_PORT:
    name: "IMAP server port (only used if auth_type is IMAP)"
    description: "(default: 143)"
  CYPHT_IMAP_AUTH_TLS:
    name: "IMAP server encryption (only used if auth_type is IMAP)"
    description: "(default: blank)"
  CYPHT_POP3_AUTH_NAME:
    name: "POP3 server name/label in UI (only used if auth_type is POP3)"
    description: "(default: localhost)"
  CYPHT_POP3_AUTH_SERVER:
    name: "POP3 server (only used if auth_type is POP3)"
    description: "(default: localhost)"
  CYPHT_POP3_AUTH_PORT:
    name: "POP3 server port (only used if auth_type is POP3)"
    description: "(default: 110)"
  CYPHT_POP3_AUTH_TLS:
    name: "POP3 server encryption (only used if auth_type is POP3)"
    description: "(default: blank)"
  CYPHT_DEFAULT_SMTP_NAME:
    name: "Default SMTP server name/label in UI for all users (only makes sense if auth_type is IMAP or POP3)"
    description: "(default: blank)"
  CYPHT_DEFAULT_SMTP_SERVER:
    name: "Default SMTP server for all users (only makes sense if auth_type is IMAP or POP3)"
    description: "(default: blank)"
  CYPHT_DEFAULT_SMTP_PORT:
    name: "Default SMTP server port for all users (only makes sense if auth_type is IMAP or POP3)"
    description: "(default: blank)"
  CYPHT_DEFAULT_SMTP_TLS:
    name: "Default SMTP server encryption for all users (only makes sense if auth_type is IMAP or POP3)"
    description: "(default: blank)"
  CYPHT_DEFAULT_SMTP_NO_AUTH:
    name: "Whether or not default SMTP server for all users requires authentication (only makes sense if auth_type is IMAP or POP3)"
    description: "(default: blank)"
  CYPHT_USER_CONFIG_TYPE:
    name: "Where user settings are stored"
    description: "(default: file)"
  CYPHT_USER_SETTINGS_DIR:
    name: "Directory for storing user settings (only used if user_config_type is file)"
    description: "(default: /var/lib/hm3/users)"
  CYPHT_ATTACHMENT_DIR:
    name: "Directory for temporarily storing attachments of outgoing messages"
    description: "(default: /var/lib/hm3/attachments)"
  CYPHT_APP_DATA_DIR:
    name: "Directory for storing module configuration files"
    description: "(default: /var/lib/hm3/app_data)"
  CYPHT_DISABLE_ORIGIN_CHECK:
    name: "Disable origin header check"
    description: "(default: false)"
  CYPHT_ADMIN_USERS:
    name: "Comma-delimited list of admin users receiving special rights"
    description: "(default: blank)"
  CYPHT_COOKIE_DOMAIN:
    name: "Domain to be set in cookie domain property"
    description: "(default: blank)"
  CYPHT_DEFAULT_EMAIL_DOMAIN:
    name: "Default email domain for users that do not login with full IMAP email address"
    description: "(default: blank)"
  CYPHT_REDIRECT_AFTER_LOGIN:
    name: "Force redirect to specific page after login"
    description: "(default: blank)"
  CYPHT_APP_NAME:
    name: "Label that appears to UI"
    description: "(default: Cypht)"
  CYPHT_DEFAULT_LANGUAGE:
    name: "Default language for users that have not set a language"
    description: "(default: en)"
  CYPHT_JS_COMPRESS:
    name: "Optionally compress JS resources"
    description: "(default: false)"
  CYPHT_CSS_COMPRESS:
    name: "Optionally compress CSS resources"
    description: "(default: false)"
  CYPHT_ENABLE_MEMCACHED:
    name: "Enable memcached to cache data"
    description: "(default: true)"
  CYPHT_MEMCACHED_SERVER:
    name: "Memcached server"
    description: "(default: 127.0.0.1)"
  CYPHT_MEMCACHED_PORT:
    name: "Memcached server port"
    description: "(default: 11211)"
  CYPHT_MEMCACHED_AUTH:
    name: "Enable SASL authentication for memcached"
    description: "(default: false)"
  CYPHT_MEMCACHED_USER:
    name: "Memcached server SASL authentication username"
    description: "(default: blank)"
  CYPHT_MEMCACHED_PASS:
    name: "Memcached server SASL authentication password"
    description: "(default: blank)"
  CYPHT_ALLOW_LONG_SESSION:
    name: "Keep user logged in across multiple browser sessions"
    description: "(default: false)"
  CYPHT_LONG_SESSION_LIFETIME:
    name: "Number of days users will remain logged in (only used if allow_long_session is true)"
    description: "(default: 30)"
  CYPHT_ENCRYPT_AJAX_REQUESTS:
    name: "Encrypt AJAX responses (adds 70KB to page load size)"
    description: "(default: blank)"
  CYPHT_ENCRYPT_LOCAL_STORAGE:
    name: "Encrypt browser local storage (adds 70KB to page load size)"
    description: "(default: blank)"
  CYPHT_DISABLE_IP_CHECK:
    name: "Disable automatic logout if IP address changes"
    description: "(default: false)"
  CYPHT_ALLOW_EXTERNAL_IMAGE_SOURCES:
    name: "Allow external images to be loaded when viewing an HTML formatted E-mail message"
    description: "(default: false)"
  CYPHT_SINGLE_SERVER_MODE:
    name: "Restrict Cypht to single email source and default SMTP server"
    description: "(default: false)"
  CYPHT_DISABLE_EMPTY_SUPERGLOBALS:
    name: "Do not empty PHP super globals for easier 3rd party integration"
    description: "(default: false)"
  CYPHT_DISABLE_OPEN_BASEDIR:
    name: "Do not apply open basedir restrictions for easier 3rd party integration"
    description: "(default: false)"
  CYPHT_DISABLE_INI_SETTINGS:
    name: "Do not tweak ini settings for easier 3rd party integration"
    description: "(default: false)"
  CYPHT_DISABLE_FINGERPRINT:
    name: "Do not user browser fingerprint for easier 3rd party integration"
    description: "(default: false)"
  CYPHT_DB_CONNECTION_TYPE:
    name: "Database connection type if database is used for authentication, sessions, or user settings"
    description: "(default: host)"
  CYPHT_DB_HOST:
    name: "Database host or IP address (only used if db_connection_type is 'host')"
    description: "(default: 127.0.0.1)"
  CYPHT_DB_SOCKET:
    name: "Database socket (only used if db_connection_type is 'socket')"
    description: "(default: /var/lib/mysqld/mysqld.sock)"
  CYPHT_DB_NAME:
    name: "Name of the database"
    description: "(default: test)"
  CYPHT_DB_USER:
    name: "User for connecting to database"
    description: "(default: test)"
  CYPHT_DB_PASS:
    name: "Password for user connecting to database"
    description: "(default: 123456)"
  CYPHT_DB_DRIVER:
    name: "Type of database driver"
    description: "(default: mysql)"
  CYPHT_MODULE_CORE:
    name: "Enable/disable Cypht core (must always be enabled)"
    description: "(default: enable)"
  CYPHT_MODULE_CONTACTS:
    name: "Enable/disable support for contacts (requires ldap_contacts, gmail_contacts, or local_contacts to be enabled as well)"
    description: "(default: enable)"
  CYPHT_MODULE_LOCAL_CONTACTS:
    name: "Enable/disable support for local storage for contacts"
    description: "(default: enable)"
  CYPHT_MODULE_LDAP_CONTACTS:
    name: "Enable/disable support for LDAP server contacts"
    description: "(default: disable)"
  CYPHT_MODULE_GMAIL_CONTACTS:
    name: "Enable/disable support for read-only Gmail contacts"
    description: "(default: disable)"
  CYPHT_MODULE_FEEDS:
    name: "Enable/disable support for RSS/ATOM feed"
    description: "(default: enable)"
  CYPHT_MODULE_POP3:
    name: "Enable/disable support for POP3 email accounts"
    description: "(default: enable)"
  CYPHT_MODULE_IMAP:
    name: "Enable/disable support for IMAP email accounts"
    description: "(default: enable)"
  CYPHT_MODULE_2FA:
    name: "Enable/disable support for two-factor authentication using TOTP"
    description: "(default: disable)"
  CYPHT_MODULE_SMTP:
    name: "Enable/disable support for outbound email via SMTP"
    description: "(default: enable)"
  CYPHT_MODULE_ACCOUNT:
    name: "Enable/disable support for creating accounts and user password updates (only used if auth_type is DB)"
    description: "(default: enable)"
  CYPHT_MODULE_IDLE_TIMER:
    name: "Enable/disable support for automatic logout when idle"
    description: "(default: enable)"
  CYPHT_MODULE_CALENDAR:
    name: "Enable/disable support for basic calendar"
    description: "(default: enable)"
  CYPHT_MODULE_THEMES:
    name: "Enable/disable support for updating UI via css"
    description: "(default: enable)"
  CYPHT_MODULE_NUX:
    name: "Enable/disable support for new user experience (common email services and development updates)"
    description: "(default: enable)"
  CYPHT_MODULE_DEVELOPER:
    name: "Enable/disable support for resources and install details for debugging"
    description: "(default: enable)"
  CYPHT_MODULE_GITHUB:
    name: "Enable/disable support for Github repository tracking"
    description: "(default: disable)"
  CYPHT_MODULE_RECAPTCHA:
    name: "Enable/disable support for Recaptcha on login"
    description: "(default: disable)"
  CYPHT_MODULE_WORDPRESS:
    name: "Enable/disable support for WordPress.com notifications"
    description: "(default: disable)"
  CYPHT_MODULE_HISTORY:
    name: "Enable/disable support for list of message read since login"
    description: "(default: enable)"
  CYPHT_MODULE_SAVED_SEARCHES:
    name: "Enable/disable support for saving and re-running searches easily"
    description: "(default: enable)"
  CYPHT_MODULE_NASA:
    name: "Enable/disable support for NASA Astronomy Picture of the Day"
    description: "(default: disable)"
  CYPHT_MODULE_PROFILES:
    name: "Enable/disable support for profiles setting reply-to, name, and signatures"
    description: "(default: enable)"
  CYPHT_MODULE_INLINE_MESSAGE:
    name: "Enable/disable support for viewing messages in an inline reading-pane rather than new page"
    description: "(default: enable)"
  CYPHT_MODULE_IMAP_FOLDERS:
    name: "Enable/disable support for adding/renaming/deleting folders in IMAP accounts"
    description: "(default: enable)"
  CYPHT_MODULE_KEYBOARD_SHORTCUTS:
    name: "Enable/disable support for keyboard shortcuts for navigations and actions"
    description: "(default: enable)"
  CYPHT_MODULE_SITE:
    name: "Enable/disable support for site-specific overrides (without hacking core code)"
    description: "(default: disable)"
  CYPHT_MODULE_DYNAMIC_LOGIN:
    name: "Enable/disable support for authenticating against populate mail services (only used if auth_type is dynamic)"
    description: "(default: disable)"
  CYPHT_MODULE_API_LOGIN:
    name: "Enable/disable support for API based login that returns JSON response"
    description: "(default: disable)"
  CYPHT_API_LOGIN_KEY:
    name: "API key for api login"
    description: "(default: blank)"
  CYPHT_MODULE_RECOVER_SETTINGS:
    name: "Enable/disable support for recovering encrypted user settings after IMAP/POP3 password change"
    description: "(default: disable)"
  CYPHT_MODULE_HELLO_WORLD:
    name: "Enable/disable support for the example module set with lots of comments"
    description: "(default: disable)"
  CYPHT_MODULE_DESKTOP_NOTIFICATIONS:
    name: "Enable/disable support for desktop notifications"
    description: "(default: disable)"
  CYPHT_DEFAULT_SETTING_NO_PASSWORD_SAVE:
    name: "Default for saving passwords between logins"
    description: "(default: false)"
  CYPHT_DEFAULT_SETTING_IMAP_PER_PAGE:
    name: "Default for number of messages per page for IMAP folders"
    description: "(default: 20)"
  CYPHT_DEFAULT_SETTING_SIMPLE_MSG_PARTS:
    name: "Default for IMAP message structure detail on message view page"
    description: "(default: false)"
  CYPHT_DEFAULT_SETTING_MSG_PART_ICONS:
    name: "Default for showing icons for each IMAP message part type"
    description: "(default: true)"
  CYPHT_DEFAULT_SETTING_TEXT_ONLY:
    name: "Default for preferring text part when viewing a message"
    description: "(default: false)"
  CYPHT_DEFAULT_SETTING_SENT_PER_SOURCE:
    name: "Default for per-source max for combined sent view"
    description: "(default: 20)"
  CYPHT_DEFAULT_SETTING_SENT_SINCE:
    name: "Default for per-source time limit for combined sent view"
    description: "(default: '-1 week')"
  CYPHT_DEFAULT_SETTING_SHOW_LIST_ICONS:
    name: "Default for displaying source icons in message lists"
    description: "(default: true)"
  CYPHT_DEFAULT_SETTING_START_PAGE:
    name: "Default for redirect page after login"
    description: "(default: none)"
  CYPHT_DEFAULT_SETTING_DISABLE_DELETE_PROMPT:
    name: "Default for prompting when deleting something"
    description: "(default: false)"
  CYPHT_DEFAULT_SETTING_NO_FOLDER_ICONS:
    name: "Default for hiding icons in the folder list"
    description: "(default: false)"
  CYPHT_DEFAULT_SETTING_ALL_EMAIL_PER_SOURCE:
    name: "Default for per-source max for all email combined view"
    description: "(default: 20)"
  CYPHT_DEFAULT_SETTING_ALL_EMAIL_SINCE:
    name: "Default for per-source time limit for all email combined view"
    description: "(default: '-1 week')"
  CYPHT_DEFAULT_SETTING_ALL_SINCE:
    name: "Default for per-source time limit for everything combined view"
    description: "(default: '-1 week')"
  CYPHT_DEFAULT_SETTING_ALL_PER_SOURCE:
    name: "Default for per-source max for everything combined view"
    description: "(default: 20)"
  CYPHT_DEFAULT_SETTING_UNREAD_PER_SOURCE:
    name: "Default for per-source max for unread combined view"
    description: "(default: 20)"
  CYPHT_DEFAULT_SETTING_FLAGGED_PER_SOURCE:
    name: "Default for per-source max for flagged combined view"
    description: "(default: 20)"
  CYPHT_DEFAULT_SETTING_FLAGGED_SINCE:
    name: "Default for per-source time limit for flagged combined view"
    description: "(default: '-1 week')"
  CYPHT_DEFAULT_SETTING_UNREAD_SINCE:
    name: "Default for per-source time limit for unread combined view"
    description: "(default: '-1 week')"
  CYPHT_DEFAULT_SETTING_TIMEZONE:
    name: "Default for timezone used to display dates"
    description: "(default: 'UTC')"
  CYPHT_DEFAULT_SETTING_LIST_STYLE:
    name: "Default for message list format"
    description: "(default: 'email_style')"
  CYPHT_DEFAULT_SETTING_LANGUAGE:
    name: "Default for interface language"
    description: "(default: 'en')"
  CYPHT_DEFAULT_SETTING_UNREAD_EXCLUDE_FEEDS:
    name: "Default for hiding news feed items from unread combined view"
    description: "(default: false)"
  CYPHT_DEFAULT_SETTING_FEED_LIMIT:
    name: "Default for per-source max for news feeds"
    description: "(default: 20)"
  CYPHT_DEFAULT_SETTING_FEED_SINCE:
    name: "Default for per-source time limit for news feeds"
    description: "(default: '-1 week')"
  CYPHT_DEFAULT_SETTING_SMTP_COMPOSE_TYPE:
    name: "Default for plain text or HTML formatted email on compose page"
    description: "(default: 0)"
  CYPHT_DEFAULT_SETTING_SMTP_AUTO_BCC:
    name: "Default for automatically BCCing sender on outbound email"
    description: "(default: false)"
  CYPHT_DEFAULT_SETTING_THEME:
    name: "Default for UI theme"
    description: "(default: 'default')"
  CYPHT_DEFAULT_SETTING_UNREAD_EXCLUDE_WORDPRESS:
    name: "Default for hiding WordPress notifications from the unread combined view"
    description: "(default: false)"
  CYPHT_DEFAULT_SETTING_WORDPRESS_SINCE:
    name: "Default for time limit for WordPress notifications"
    description: "(default: '-1 week')"
  CYPHT_DEFAULT_SETTING_UNREAD_EXCLUDE_GITHUB:
    name: "Default for hiding GitHub notifications from the unread combined view"
    description: "(default: false)"
  CYPHT_DEFAULT_SETTING_GITHUB_LIMIT:
    name: "Default for per-source max for GitHub notifications"
    description: "(default: 20)"
  CYPHT_DEFAULT_SETTING_GITHUB_SINCE:
    name: "Default for time limit for GitHub notifications"
    description: "(default: '-1 weeks')"
  CYPHT_DEFAULT_SETTING_INLINE_MESSAGE:
    name: "Default for displaying message details inline from message list"
    description: "(default: false)"
  CYPHT_DEFAULT_SETTING_ENABLE_KEYBOARD_SHORTCUTS:
    name: "Default for enabling keyboard shortcuts"
    description: "(default: 1)"
